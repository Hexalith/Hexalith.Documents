@inherits HexAutocomplete

<HexAutocomplete AdditionalAttributes="@AdditionalAttributes"
                 Appearance="@Appearance"
                 AriaLabel="@AriaLabel"
                 AutoComplete="@AutoComplete"
                 Autofocus="@Autofocus"
                 ChildContent="@(ChildContent!)"
                 Class="@Class"
                 Data="@Data"
                 Disabled="@Disabled"
                 DisplayName="@DisplayName"
                 Embedded="@Embedded"
                 Field="@Field"
                 FooterContent="@(FooterContent!)"
                 HeaderContent="@(HeaderContent!)"
                 Height="@Height"
                 IconDismiss="@IconDismiss"
                 IconSearch="@IconSearch"
                 Id="@Id"
                 Immediate="@Immediate"
                 ImmediateDelay="@ImmediateDelay"
                 Items="@Items"
                 ItemSize="@ItemSize"
                 KeepOpen="@KeepOpen"
                 Label="@(Label ?? ((MaximumSelectedOptions.HasValue && MaximumSelectedOptions.Value == 1) ? Labels.Label : Labels.LabelPlural))"
                 LabelTemplate="@(LabelTemplate!)"
                 MaxAutoHeight="@MaxAutoHeight"
                 MaximumOptionsSearch="@MaximumOptionsSearch"
                 MaximumSelectedOptions="@int.MaxValue"
                 MaximumSelectedOptionsMessage="@(MaximumSelectedOptionsMessage!)"
                 Name="@Name"
                 OnOptionsSearch="@OnOptionsSearch"
                 OptionClass="@OptionClass"
                 OptionStyle="@OptionStyle"
                 OptionTemplate="@(OptionTemplate!)"
                 ParentReference="@ParentReference"
                 Placeholder="@(Placeholder ?? Labels.Placeholder)"
                 ReadOnly="@ReadOnly"
                 Required="@Required"
                 SelectedOption="@SelectedOption"
                 SelectedOptionChanged="@SelectedOptionChanged"
                 SelectedOptions="@SelectedOptions"
                 SelectedOptionsChanged="@SelectedOptionsChanged"
                 SelectedOptionTemplate="@(SelectedOptionTemplate!)"
                 SelectValueOnTab="@SelectValueOnTab"
                 ShowOverlayOnEmptyResults="@ShowOverlayOnEmptyResults"
                 Style="@Style"
                 TitleScrollToNext="@TitleScrollToNext"
                 TitleScrollToPrevious="@TitleScrollToPrevious"
                 ValueExpression="@ValueExpression"
                 ValueText="@ValueText"
                 ValueTextChanged="@ValueTextChanged"
                 Virtualize="@Virtualize"
                 Width="@Width">
</HexAutocomplete>

@code
{
}
